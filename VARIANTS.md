# Варианты лендинга VERANDARU

| Вариант | Git-ветка    | URL (локально / прод)     |
|---------|--------------|---------------------------|
| **1**   | `variant-1`  | `/`                       |
| **2**   | `variant-2`  | `/v2`                     |
| **3**   | `variant-3`  | `/v3`                     |
| **4**   | `variant-4`  | `/v4`                     |
| **5**   | `variant-5`  | `/v5`                     |

- **`/`** — вариант 1 (светлый editorial)
- **`/v2`** — вариант 2 (Feadship, компас-интро)
- **`/v3`** — копия v2; ТЗ — `docs/V3_BRIEF.md`
- **`/v4`** — Yacht Edition по ТЗ — `docs/V4_BRIEF.md`
- **`/v5`** — копия v4 + экскурсия Yacht Spaces (полноэкранные зоны)

Старый путь `/variant-2` редиректит на `/v2`.

В подвале v1–v5: переключатель версий (техн., потом уберём).

В заявках в Telegram: **Вариант 1–5**.

## Ветки

```bash
# Вариант 1 (текущая стабильная копия)
git checkout variant-1

# Вариант 2 (эксперименты)
git checkout variant-2
```

## Vercel

- Production с ветки `variant-1` → основной домен, `/`
- Preview с ветки `variant-2` → preview URL, `/v2`
